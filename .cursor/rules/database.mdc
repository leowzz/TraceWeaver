---
alwaysApply: true
globs: ["backend/app/models/**/*.py", "backend/alembic/**/*.py"]
---

# 数据库规则

## 模型定义

### 虚拟外键（无实体约束）
```python
class Activity(SQLModel, table=True):
    user_id: str = Field(index=True)  # ✅ 只加索引
    source_config_id: Optional[int] = Field(default=None, index=True)
    # user_id: str = Field(foreign_key="user.id")  # ❌ 禁止
```

### JSONB 字段
```python
from sqlalchemy import Column
from sqlalchemy.dialects.postgresql import JSONB

extra_data: dict = Field(default_factory=dict, sa_column=Column(JSONB))
```

### 向量字段
```python
from pgvector.sqlalchemy import Vector

embedding: List[float] = Field(sa_column=Column(Vector(1536)))

# 向量索引
__table_args__ = (
    Index("idx_embedding_vector", "embedding",
          postgresql_using="hnsw",
          postgresql_with={"m": 16, "ef_construction": 64},
          postgresql_ops={"embedding": "vector_cosine_ops"}),
)
```

### 索引设计
- 单列：`Field(index=True)`、`Field(unique=True)`
- 复合索引在 `__table_args__` 中定义
- 顺序：等值查询列在前，范围查询列在后

## Alembic 迁移

### AI 行为规范
- **不自动生成迁移脚本内容**
- 检测到 Model 变更时提醒：
  ```
  ⚠️ 检测到数据库模型变更
  请手动执行：
  cd backend
  alembic revision --autogenerate -m "描述"
  ```

### 审核清单
1. 表名和列名是否正确
2. 索引是否必要
3. **确认无外键约束**（项目规范）
4. 数据迁移逻辑是否安全
5. 测试环境验证后再上生产

### 大表迁移
- 添加列：先 `nullable=True`，分批填充，再改 `NOT NULL`
- 创建索引：使用 `postgresql_concurrently=True`（需在事务外）
- 分批更新：循环 `LIMIT` 避免长时间锁表

### 常见问题
- **pgvector 扩展**：手动执行 `CREATE EXTENSION vector;`，不通过 Alembic
- **ENUM 变更**：需要创建新类型，转换列，删除旧类型，重命名
- **回滚**：`alembic downgrade -1` 或 `alembic downgrade <revision_id>`
