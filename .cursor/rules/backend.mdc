---
alwaysApply: true
globs: ["backend/**/*.py"]
---

# 后端开发规则

## ORM 操作规范

### 使用同步 Session

**必须使用同步方式**操作数据库，不使用 AsyncSession：

```python
# ✅ 正确：使用同步 Session
from sqlmodel import Session, select
from app.core.db import engine

def get_activities(user_id: str):
    with Session(engine) as session:
        statement = select(Activity).where(Activity.user_id == user_id)
        results = session.exec(statement).all()
        return results

# ❌ 错误：不使用 async/await
async def get_activities(user_id: str):
    async with AsyncSession(engine) as session:  # 禁止
        ...
```

### 依赖注入

在 API 路由中使用依赖注入获取 Session：

```python
from fastapi import Depends
from app.api.deps import get_db

@router.get("/activities")
def list_activities(
    session: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    statement = select(Activity).where(Activity.user_id == current_user.id)
    activities = session.exec(statement).all()
    return activities
```

## 类型注解规范

### 使用 Optional[T] 而非 T | None

```python
from typing import Optional

# ✅ 正确
def get_user(user_id: str) -> Optional[User]:
    ...

class Activity(SQLModel, table=True):
    content: Optional[str] = None
    source_config_id: Optional[int] = Field(default=None)

# ❌ 错误
def get_user(user_id: str) -> User | None:  # 禁止
    ...
```

### 完整的类型注解

所有函数和方法必须有完整的类型注解：

```python
# ✅ 正确
def calculate_total(items: List[Item], tax_rate: float = 0.1) -> Decimal:
    ...

# ❌ 错误：缺少类型注解
def calculate_total(items, tax_rate=0.1):  # 禁止
    ...
```

## Pydantic Schema 规范

### 复杂参数封装

不要传递裸的 dict，使用 Pydantic Schema：

```python
from pydantic import BaseModel

# ✅ 正确：使用 Schema
class GitConfig(BaseModel):
    repo_path: str
    branch: str = "main"

def sync_git_repo(config: GitConfig) -> List[Activity]:
    path = config.repo_path  # 类型安全
    ...

# ❌ 错误：使用 dict
def sync_git_repo(config: dict) -> List[Activity]:  # 禁止
    path = config["repo_path"]  # 不安全，容易出错
    ...
```

### Schema 命名约定

遵循以下命名模式：

- `XxxCreate` - 创建请求
- `XxxUpdate` - 更新请求
- `XxxPublic` - API 响应
- `XxxsPublic` - 列表响应
- `XxxInDB` - 数据库内部模型（少用）

```python
# 例子
class ActivityCreate(BaseModel):
    source_type: SourceType
    title: str
    content: Optional[str] = None
    ...

class ActivityUpdate(BaseModel):
    title: Optional[str] = None
    content: Optional[str] = None

class ActivityPublic(BaseModel):
    id: int
    title: str
    occurred_at: datetime
    ...

class ActivitiesPublic(BaseModel):
    data: List[ActivityPublic]
    count: int
```

## 日志记录规范

### 使用 loguru

项目统一使用 loguru 作为日志框架：

```python
from loguru import logger

# ✅ 正确：使用 loguru
logger.info("User {user_id} syncing {count} activities", user_id=user.id, count=len(activities))
logger.error("Failed to connect to SiYuan: {error}", error=str(e))
logger.debug("Connector config: {config}", config=config.dict())

# ❌ 错误：使用标准库 logging
import logging  # 禁止在业务代码中使用
logging.info("User syncing activities")
```

### 日志级别使用

- `logger.debug()` - 调试信息（开发环境）
- `logger.info()` - 关键业务流程（生产环境可见）
- `logger.warning()` - 警告但可恢复的情况
- `logger.error()` - 错误但不影响整体系统
- `logger.critical()` - 严重错误，系统不可用

### 结构化日志

使用占位符而非字符串拼接：

```python
# ✅ 正确
logger.info("Activity {id} embedded into {chunks} chunks", id=activity.id, chunks=len(chunks))

# ❌ 错误
logger.info(f"Activity {activity.id} embedded into {len(chunks)} chunks")  # 不利于日志分析
```

## 数据库模型规范

### 无实体外键约束

使用虚拟外键，不添加数据库层面的 FOREIGN KEY 约束：

```python
# ✅ 正确：虚拟外键
class Activity(SQLModel, table=True):
    user_id: UUID = Field(index=True)  # 只加索引，不加外键
    source_config_id: Optional[int] = Field(default=None, index=True)

# ❌ 错误：实体外键
class Activity(SQLModel, table=True):
    user_id: UUID = Field(foreign_key="user.id")  # 禁止
```

### JSONB 存储扩展数据

使用 JSONB 字段存储数据源特有的元数据：

```python
from sqlalchemy import Column
from sqlalchemy.dialects.postgresql import JSONB

class Activity(SQLModel, table=True):
    extra_data: dict = Field(
        default_factory=dict,
        sa_column=Column(JSONB)
    )
```

### 时间戳字段

使用 `datetime.now`（非 `datetime.utcnow`）：

```python
from datetime import datetime

class Activity(SQLModel, table=True):
    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)
```

## Alembic 迁移规范

### 手动生成迁移脚本

**AI 不应自动生成 Alembic 脚本**，只做提醒：

```
✅ 提示用户：
"检测到 Model 变更，请手动执行以下命令生成迁移脚本：
cd backend
alembic revision --autogenerate -m '添加 activity_embedding 表'

生成后请仔细审核 alembic/versions/ 下的脚本文件。"

❌ 不要直接生成迁移脚本内容
```

### 迁移脚本审核清单

提醒用户审核以下内容：

1. 表名和列名是否正确
2. 索引是否必要
3. 数据迁移逻辑是否安全
4. 是否需要回滚逻辑
5. 生产环境数据是否已备份

## 连接器开发规范

### 实现 BaseConnector 接口

所有数据源连接器必须继承并实现 `BaseConnector`：

```python
from app.connectors.base import BaseConnector
from app.schemas.activity import ActivityCreate

class MyConnector(BaseConnector):
    def __init__(self, config: MyConfig):
        super().__init__(config)
        self.config: MyConfig = config

    @property
    def source_type(self) -> str:
        return SourceType.MY_SOURCE.value

    async def validate_config(self) -> bool:
        """验证配置有效性"""
        # 实现验证逻辑
        return True

    async def fetch_activities(
        self,
        start_time: datetime,
        end_time: datetime,
    ) -> List[ActivityCreate]:
        """抓取活动数据"""
        # 实现数据抓取和转换
        return activities
```

### 配置 Schema 设计

每个连接器配置使用独立的 Pydantic Schema：

```python
from pydantic import BaseModel, HttpUrl

class SiYuanConfig(BaseModel):
    api_url: HttpUrl
    api_token: str

    class Config:
        json_schema_extra = {
            "example": {
                "api_url": "http://localhost:6806",
                "api_token": "your-token-here"
            }
        }
```

## 异常处理规范

### 使用自定义异常

定义清晰的异常类型：

```python
from app.core.exceptions import (
    ConnectorError,
    ConfigValidationError,
    DataSourceUnavailableError,
)

# ✅ 正确
async def validate_config(self) -> bool:
    try:
        # 验证逻辑
        ...
    except httpx.ConnectError as e:
        raise DataSourceUnavailableError(
            f"Cannot connect to {self.config.api_url}: {e}"
        )
    except ValueError as e:
        raise ConfigValidationError(f"Invalid config: {e}")
```

### 记录异常上下文

```python
try:
    activities = connector.fetch_activities(start, end)
except ConnectorError as e:
    logger.error(
        "Failed to fetch from {source}: {error}",
        source=source_config.name,
        error=str(e),
        exc_info=True,  # 包含堆栈信息
    )
    raise
```

## 服务层设计规范

### 服务类职责单一

每个服务类专注一个业务领域：

```python
# ✅ 正确：职责清晰
class ActivityService:
    def __init__(self, db: Session):
        self.db = db

    def sync_source(self, config: SourceConfig) -> List[Activity]:
        ...

    def get_activities(self, user_id: str, filters: ActivityFilter) -> List[Activity]:
        ...

class EmbeddingService:
    def __init__(self, embedder_config: EmbedderConfig):
        self.embedder = EmbedderFactory.create(embedder_config)

    def embed_activity(self, activity: Activity) -> List[ActivityEmbedding]:
        ...
```

### 服务间依赖注入

避免循环依赖，使用构造函数注入：

```python
class ReportService:
    def __init__(
        self,
        db: Session,
        activity_service: ActivityService,
        llm_service: LLMService,
    ):
        self.db = db
        self.activity_service = activity_service
        self.llm_service = llm_service
```

## 测试规范

### 使用 pytest

```python
import pytest
from sqlmodel import Session

def test_activity_creation(session: Session):
    activity = Activity(
        user_id="test-user",
        source_type=SourceType.GIT,
        title="Test commit",
        fingerprint="test-fingerprint",
    )
    session.add(activity)
    session.commit()
    session.refresh(activity)

    assert activity.id is not None
    assert activity.title == "Test commit"
```

### Mock 外部依赖

```python
from unittest.mock import Mock, patch

def test_git_connector():
    config = GitConfig(repo_path="/tmp/test-repo", branch="main")
    connector = GitConnector(config)

    with patch("git.Repo") as mock_repo:
        # Mock 外部 Git 库
        mock_repo.return_value.iter_commits.return_value = []
        activities = await connector.fetch_activities(start, end)

    assert len(activities) == 0
```

## 性能优化建议

### 批量操作

使用批量插入而非逐条插入：

```python
# ✅ 正确：批量插入
session.add_all(activities)
session.commit()

# ❌ 低效：逐条插入
for activity in activities:
    session.add(activity)
    session.commit()  # 每次都提交
```

### 使用 select 而非 query

SQLModel 推荐使用 `select`：

```python
# ✅ 正确
from sqlmodel import select

statement = select(Activity).where(Activity.user_id == user_id)
activities = session.exec(statement).all()

# ⚠️ 旧风格（兼容但不推荐）
activities = session.query(Activity).filter(Activity.user_id == user_id).all()
```
