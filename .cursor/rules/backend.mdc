---
alwaysApply: true
globs: ["backend/**/*.py"]
---

# 后端开发规则

## ORM 操作

- 支持**同步**和**异步**两种方式操作数据库
- 简单 CRUD 优先使用同步 Session
- I/O 密集型操作、并发查询使用异步 AsyncSession
- 使用 `select()` 而非 `.query()`
- 批量操作使用 `session.add_all()` + `session.commit()`

```python
# 同步
from sqlmodel import Session, select
with Session(engine) as session:
    result = session.exec(select(Activity).where(...)).all()

# 异步
from sqlmodel.ext.asyncio.session import AsyncSession
async with AsyncSession(async_engine) as session:
    result = await session.exec(select(Activity).where(...))
    activities = result.all()
```

## CRUD 操作

- **优先使用 CRUD 对象**简化数据库操作
- 项目在 `app/crud/` 定义了基于 `CRUDBase` 的 CRUD 类
- 避免在路由或服务层直接写 SQL 查询

```python
# ✅ 推荐：使用 CRUD 对象
from app.crud.activity import activity_crud

# 在 API 路由中
@router.get("/activities")
def list_activities(session: Session = Depends(get_db), skip: int = 0, limit: int = 20):
    activities = activity_crud.get_multi(session, skip=skip, limit=limit)
    return activities

@router.post("/activities")
def create_activity(activity: ActivityCreate, session: Session = Depends(get_db)):
    new_activity = activity_crud.create(session, obj_in=activity)
    return new_activity

# ❌ 避免：在路由中直接写查询
@router.get("/activities")
def list_activities(session: Session = Depends(get_db)):
    result = session.exec(select(Activity).offset(0).limit(20))
    return result.all()
```

### 自定义 CRUD 方法

当需要复杂查询时，在 CRUD 类中添加方法：

```python
# app/crud/activity.py
class ActivityCRUD(CRUDBase[Activity, ActivityCreate, ActivityUpdate]):
    def get_by_user_and_source(
        self, 
        session: Session, 
        user_id: str, 
        source_type: SourceType
    ) -> List[Activity]:
        statement = select(Activity).where(
            Activity.user_id == user_id,
            Activity.source_type == source_type
        )
        return session.exec(statement).all()
```

## 类型注解

- 使用 `Optional[T]` 而非 `T | None`
- 所有函数必须有完整类型注解
- 复杂参数封装为 Pydantic Schema，不传递裸 dict

```python
from typing import Optional
from pydantic import BaseModel

class GitConfig(BaseModel):
    repo_path: str
    branch: str = "main"

def sync_repo(config: GitConfig) -> List[Activity]:  # ✅
    ...
```

## 日志记录

- 统一使用 **loguru**
- 使用占位符而非 f-string：`logger.info("User {user_id} synced", user_id=user.id)`
- 异常记录：`logger.exception(f"{e=}")`

## 数据库模型

- **无实体外键约束**：只用虚拟外键 `Field(index=True)`
- **JSONB 存储元数据**：`sa_column=Column(JSONB)`
- **时间戳**：使用 `datetime.now` 而非 `utcnow`
- **向量字段**：使用 pgvector 的 `Vector(1536)`

## Alembic 迁移

- **AI 不生成迁移脚本**，提醒用户手动执行：
  ```bash
  cd backend
  alembic revision --autogenerate -m "描述"
  alembic upgrade head
  ```
- 检查：表名、列类型、索引、无外键约束
- 大表操作：先 nullable=True，分批填充，再改 NOT NULL
- 在线索引：使用 `postgresql_concurrently=True`

## 服务层设计

- 服务类职责单一
- 支持同步和异步两种实现
- 构造函数注入依赖，避免循环依赖

## 连接器开发

- 继承并实现 `BaseConnector` 接口
- 实现 `source_type`、`validate_config()`、`fetch_activities()`
- 配置使用独立的 Pydantic Schema

## 异常处理

- 使用自定义异常类型（`ConnectorError`、`ConfigValidationError`）
- 记录异常上下文：`logger.error(..., exc_info=True)`

## 性能优化

- 异步并发查询：使用 `asyncio.gather()`
- 连接池配置：合理设置 `pool_size`、`max_overflow`
- 避免在异步代码中使用阻塞 I/O（如 `requests`，应用 `httpx`）
