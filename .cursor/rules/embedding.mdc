---
alwaysApply: true
globs: ["backend/app/services/*embedding*.py", "backend/app/**/*embedding*.py"]
---

# 向量化规则（Agno 框架）

## 核心原则

使用 **Agno 框架** 的 Embedder 抽象层进行文本向量化。

**为什么选择 Agno？**
- 统一接口，支持多种后端（OpenAI、Ollama、HuggingFace、Gemini、Cohere）
- 灵活切换：开发用本地模型，生产用云端 API
- 内置批量处理优化

## Embedder 配置

### 配置模型
```python
from pydantic import BaseModel
from typing import Literal, Optional

class EmbedderConfig(BaseModel):
    provider: Literal["openai", "ollama", "huggingface", "gemini", "cohere"]
    model_name: str  # 如 "text-embedding-3-small"
    dimensions: int = 1536
    api_key: Optional[str] = None
    batch_size: int = 100
    enable_batch: bool = True
```

### Embedder 工厂
```python
class EmbedderFactory:
    @staticmethod
    def create(config: EmbedderConfig):
        if config.provider == "openai":
            return OpenAIEmbedder(id=config.model_name, ...)
        elif config.provider == "ollama":
            return OllamaEmbedder(id=config.model_name, ...)
        # ... 其他提供商
```

## Embedding 服务

### 核心流程
1. **文本分块**：将长文本切分（chunk_size=512, overlap=50）
2. **批量向量化**：`embedder.get_embeddings(chunks)`
3. **存储向量**：创建 `ActivityEmbedding` 记录并提交

### 检索服务
- 使用 pgvector 的 `<=>` 运算符进行余弦相似度搜索
- 相似度 = `1 - (embedding <=> query_embedding)`
- 为 LLM 构建上下文：Top-K 片段拼接成 Markdown

```python
sql = text("""
    SELECT *, 1 - (embedding <=> :query_embedding) AS similarity
    FROM activity_embedding
    WHERE user_id = :user_id
    ORDER BY embedding <=> :query_embedding
    LIMIT :top_k
""")
```

## 性能优化

- **批量处理**：启用 `enable_batch=True`
- **异步任务**：使用 Celery 异步向量化
- **缓存**：避免重复向量化相同文本
- **重试机制**：使用 tenacity 处理 API 限流

## 模型切换

- 更换 Embedder 需**重新向量化所有数据**
- 删除旧向量 → 获取活动列表 → 使用新模型批量向量化
