---
alwaysApply: true
---

# TraceWeaver 项目概览

## 定位

个人数据管理和知识库构建平台，将碎片化数据（代码提交、笔记、时间记录）汇聚成统一知识库，通过向量化和 RAG 技术实现语义搜索和智能问答。

## 核心理念

### Trace as a Stream（痕迹即流）
所有数据源统一转换为标准化的 `Activity` 事件：
```
Git 提交 → Activity
思源笔记 → Activity
时间记录 → Activity
```

### Knowledge as Context（知识即上下文）
```
数据 → 向量化 → pgvector → 语义搜索 → LLM 上下文 → 智能输出
```

## 系统架构

```
数据源（Git/SiYuan/Dayflow）
  ↓
适配器层（Connector）
  ↓
Activity 统一模型
  ↓
向量化（Agno Embedder）
  ↓
PostgreSQL + pgvector
  ↓
RAG 检索 → LLM 报告生成
```

## 技术栈

**后端：** FastAPI + SQLModel + PostgreSQL + pgvector + Agno + Loguru + Celery  
**前端：** React + TypeScript + TanStack Router/Query + shadcn/ui + Tailwind  
**基础设施：** Docker Compose + Alembic + Pytest

## 核心数据模型

```python
# Activity - 所有数据源的统一表示
class Activity(SQLModel, table=True):
    id, user_id, source_type, source_id, occurred_at, title, content
    extra_data: dict  # JSONB，存储源特有数据
    fingerprint: str  # 去重哈希

# ActivityEmbedding - 用于语义搜索
class ActivityEmbedding(SQLModel, table=True):
    activity_id, user_id, embedding, chunk_text, embedder_model

# SourceConfig - 数据源连接配置
class SourceConfig(SQLModel, table=True):
    user_id, type, name, config_payload, is_active
```

## 设计原则

1. **六边形架构**：核心业务层定义接口，适配器层实现外部系统
2. **无实体外键**：使用虚拟外键提高灵活性
3. **JSONB 存储元数据**：适配不同数据源
4. **Embedder 可切换**：通过 Agno 支持多种模型
5. **Connector 可插拔**：实现 BaseConnector 接口即可添加新数据源

## 核心业务流程

### 数据同步
用户触发 → 遍历 SourceConfig → Connector 抓取 → 转换为 Activity → 去重存储 → 异步向量化

### 向量化
Activity → 文本分块 → Agno Embedder → ActivityEmbedding → pgvector 索引

### RAG 查询
用户提问 → 查询向量化 → pgvector 相似性搜索 → Top-K 片段 → LLM Prompt → 生成报告

## 当前支持的数据源

1. Git 仓库（手动配置 + 批量扫描）
2. 思源笔记（API 同步）
3. Dayflow 时间记录

## 项目来源

基于 **Full Stack FastAPI Template** 构建。
